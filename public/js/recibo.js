import { sumar, restar } from './envio.js';

console.log("probando la funcion");

const a = 10;
const b = 5;

console.log('Suma de', a, 'y', b, 'es:', sumar(a, b));
console.log('Resta de', a, 'y', b, 'es:', restar(a, b));

console.log();
